import { CopyIcon } from "@components/icons";
import IconButton from "@components/ui/IconButton";

const CopyButton = () => {
  return <IconButton icon={<CopyIcon />} />;
};

export default CopyButton;
